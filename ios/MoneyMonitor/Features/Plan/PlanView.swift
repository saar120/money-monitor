import SwiftUI

struct PlanView: View {
    var body: some View {
        FeaturePlaceholderView(
            title: "Plan",
            systemImage: "chart.pie",
            message: "Budgets, net worth, and asset detail arrive in Phase 3."
        )
        .navigationTitle("Plan")
    }
}

