# Server
PORT=3000

# Anthropic API key for financial analysis
ANTHROPIC_API_KEY=sk-ant-...

# Encryption key for storing bank credentials at rest (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY=

# Database path (default: ./data/money-monitor.db)
DATABASE_PATH=./data/money-monitor.db
